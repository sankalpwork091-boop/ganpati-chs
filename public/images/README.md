# Images

Drop the building render here as **`ganpati-chs-building.jpg`**.

It is used as the homepage hero image (`src/lib/society.ts` → `HERO_IMAGE`).
If the file is absent the homepage falls back to a styled placeholder panel
rather than a broken image, so a fresh checkout still renders.
